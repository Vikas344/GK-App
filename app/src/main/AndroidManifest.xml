<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.custom_listview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".QuizActivity"/>
        <activity android:name=".StudyActivity" />
        <activity android:name=".QuizForHistory" />
        <activity android:name=".DiverseActivity" />
        <activity android:name=".SportsActivity" />
        <activity android:name=".GenralScienceActivity" />
        <activity android:name=".PoliticsActivity" />
        <activity android:name=".ConstitutionActivity" />
        <activity android:name=".EconomicsActivity" />
        <activity android:name=".GeographyActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>